---
description: Push all current changes to Git
---

// turbo-all
1. Stage all changes
```bash
git add .
```

2. Commit changes
```bash
git commit -m "Automated update via /git slash command"
```

3. Push to remote
```bash
git push
```
