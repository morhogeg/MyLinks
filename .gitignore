# Dependencies
node_modules/
.pnp
.pnp.js
dist/
build/
.next/
out/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# System
.DS_Store
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env
.venv
env/
ENV/
env.bak/
venv.bak/

# Firebase (if any)
.firebase/
firebaserc
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# Firebase native config — contains the API key / OAuth client id. Injected in
# CI from a base64 secret; must never be committed even if dropped in for a
# local ./build-ios.sh run.
**/GoogleService-Info.plist
google-services.json

# Apple signing material
*.p8
*.p12
*.mobileprovision

# Leftover captured API responses / debug dumps
web/output.json

# Claude Code agent worktrees (transient, auto-cleaned)
.claude/worktrees/
